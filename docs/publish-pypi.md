# `publish-pypi.yaml`

Build and publish a Python package to PyPI or TestPyPI.

## Highlights

- uses `python -m build`
- supports a test publish mode
- expects a `password` secret with the package token
