# `publish-on-release.yaml`

Automatically run tests and publish on a GitHub release.

## Highlights

- triggered by release publication
- uses `secrets: inherit`
- publishes to both TestPyPI and PyPI
